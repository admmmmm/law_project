---

# 07_真实仓库核验校正版

本节基于当前 `law_project` 仓库实际代码核验生成，用于比赛支撑材料、PPT 备注和答辩口径校准。原则是：能用代码证明的写“已实现”，只有前端展示或原型链路的写“原型 / 演示性实现”，未见真实代码的写“规划增强 / 未见实现”。

## 一、模块路径核验表

| 模块 | 支撑材料当前说法 | 真实代码路径 | 是否准确 | 需要修改的表述 | 风险 / 边界 |
|---|---|---|---|---|---|
| 材料导入与处理 | 支持文本、单文件、多文件、ZIP、结构化数据导入 | `backend/app/services/ingestion_service.py`; `backend/app/services/information_extraction.py`; `backend/app/api/v1/routes/ingestion.py`; `backend/app/schemas/ingestion.py` | 基本准确 | 可写“支持文本、文件、多文件与 ZIP 导入；支持 txt/md/csv/xlsx/json/pdf/docx，doc 暂不支持” | `.doc` 未实现；PDF/docx 解析质量依赖第三方库与文本层质量 |
| 证据编号与保存 | 生成 evidence_id 并保存原文、抽取结果 | `IngestionService._save_evidence`; `schemas/common.py:new_id`; `MemoryStore` 中 `evidence/raw_contents/extractions` 表 | 准确 | evidence_id 是 `new_id("evd")` 生成，证据原文与抽取结果进入 SQLite JSON payload | 不是传统 ORM；原型阶段以 SQLite + 内存缓存为主 |
| 文本切片与结构化行解析 | 非结构化切句，结构化行解析并转三元组 | `information_extraction.py:route_extraction`, `parse_structured_content`, `map_rows_to_triples`, `extract_unstructured_triples`, `build_passages_from_triples` | 准确 | 可写“非结构化文本按句切为 passage；CSV/XLSX 行级解析为交易、通话、时间线等关系” | 非结构化本地规则切片较简单；高质量 OpenIE 依赖后续 HippoRAG/LLM |
| 案件证据图谱体系 | 三层证据图谱：文件层、片段层、事实关系层 | `backend/app/services/evidence_map_service.py`; `backend/app/schemas/evidence_map.py`; `backend/app/api/v1/routes/graph.py:get_evidence_map`; `frontend/src/views/Graph.vue` | 准确 | 可写“EvidenceMap API 输出 documents/passages/triples/document_edges/passage_edges/containment_edges，供图谱页分层展示” | EvidenceMapBuilder 不调用 DeepSeek；质量取决于已导入证据、extractions 和已有 document mother |
| 原始案件图谱 | 构建实体/关系/线索图谱 | `backend/app/adapters/algorithm.py`; `backend/app/services/graph_service.py`; `backend/app/schemas/graph.py` | 基本准确 | 可写“算法适配层构建 InvestigationGraph，图谱服务提供查询、合并和人工干预” | 图谱质量取决于 OpenIE、结构化解析和 ontology 分类；不是 Neo4j 等专用图数据库 |
| 图谱支撑机制 | 核心邻域、邻域扩展、多跳召回、子图交叉 | `frontend/src/views/Graph.vue` 中 `buildCoreNeighborhoodSubgraph`, `buildExpandedNeighborhoodSubgraph`, `buildMultihopPathSubgraph`, `buildIntersectionSubgraph`; `frontend/src/components/G6EvidenceGraph.vue` | 需收口 | 应写“当前主要是前端基于已加载图谱数据的演示性图操作，用于演示局部关系、路径和交叉视角” | 不是后端图数据库算法；PPR 不是这些按钮的直接实现 |
| G6 图谱渲染 | 图谱可视化使用 G6 | `frontend/src/components/G6EvidenceGraph.vue`; `frontend/package.json` 依赖 `@antv/g6` | 准确 | 可写“图谱主视图已接入 AntV G6，保留 relation-graph 作为原始图谱维护 / fallback” | 仍存在 relation-graph 兼容入口；截图时应选择 G6 视图 |
| Agentic RAG | 检索计划、工具调用、循环检索、fallback | `backend/app/services/retrieval_planner_service.py`; `backend/app/schemas/analysis.py:RetrievalSession/RetrievalStep`; `MemoryStore.retrieval_sessions/retrieval_steps` | 部分准确 | 可写“实现了 planner + 工具执行 + 过程持久化；模型计划失败时回退到规则化工具计划” | 不是完整函数调用协议；部分计划由 fallback 生成 |
| 智能分析会话 | 假设验证、可疑资金流、多轮会话 | `backend/app/services/analysis_service.py:create_thread/add_thread_message/delete_thread`; `backend/app/api/v1/routes/analysis.py`; `frontend/src/views/Intelligence.vue` | 准确 | 可写“支持 hypothesis / financial_flow 两类会话，新建、追问、删除，并保存消息与检索过程” | `@xxx` 是证据 mention，不是技能包 mention |
| 检察分析 Skills | 规则包列表和运行 | `backend/app/services/analysis_skill_service.py`; `backend/app/api/v1/routes/analysis_skills.py`; `backend/app/analysis_skills/packs/*` | 部分准确 | 可写“后端已实现规则包注册、列表、运行和 bundle 运行；前端 SkillLibrary 页面存在但未在 router 注册” | `/analysis-skills/import`、`/analysis-skills/{skill}/export` 前端封装存在，后端路由未见实现；@技能包未实现 |
| 侦查画像生成 | portrait_facts 和报告 | `backend/app/services/analysis_service.py:portrait_facts`; `backend/app/services/report_service.py`; `backend/app/api/v1/routes/reports.py`; `frontend/src/views/Portrait.vue` | 准确 | 可写“画像分为事实画像 portrait_facts 与详细画像报告 reports/portrait，两者均支持证据支撑字段” | portrait_facts 依赖模型/检索，离线或超时会影响生成 |
| 证据溯源 | 分析句、画像事实、报告 claim 可溯源 | `schemas/analysis.py:GroundedSentence/PortraitFact`; `schemas/report.py:PortraitClaim`; `analysis_service._ground_answer_sentences`; `report_service._verify_claim`; `Intelligence.vue/Portrait.vue` 溯源抽屉 | 基本准确 | 可写“系统尽量为句子绑定 passage、graph path 或 evidence_id；无法匹配时支撑为空或较弱” | 不应承诺每一句都强证据支撑 |
| 法律知识库 | 罪名模板、流程、检索和上传 | `backend/app/services/legal_knowledge_service.py`; `backend/app/api/v1/routes/legal_knowledge.py`; `data/legal_knowledge`; `frontend/src/views/LegalKnowledge.vue` | 部分准确 | 后端真实接口是 `/offense-templates`, `/procedure-flows`, `/procedure-flows/upload`, `/retrieve` 等 | 前端 `backend.ts` 中 `/legal-knowledge/index/crimes/processes/upload` 封装未见后端对应路由 |
| 运行部署 | 本地启动、Caddy、环境变量 | `backend/README.md`; `backend/.env.example`; `frontend/package.json`; `Caddyfile.jcmx`; `start_jcmx.bat` | 准确 | 可写“支持本地前后端启动，也支持 build 后通过 Caddy 挂载 `/jcmx`” | 模型 Key、BGE-M3 路径、Python 3.10 环境需本机配置 |

## 二、关键代码证据清单

| 功能 | 文件路径 | 函数 / 类名 | 推荐截取代码范围 | 这段代码证明什么 | 对应章节 |
|---|---|---|---|---|---|
| 多源材料导入 | `backend/app/services/ingestion_service.py` | `IngestionService.ingest_file`, `ingest_many_files`, `ingest_archive` | 每个函数各截 10-20 行 | 证明支持单文件、多文件和 ZIP 导入 | 数据接入 |
| 文档解析与证据切片 | `backend/app/services/information_extraction.py` | `extract_content`, `route_extraction`, `extract_unstructured_triples` | `route_extraction` + `extract_unstructured_triples` | 证明按文件类型路由，非结构化材料切成 passages | 数据处理 |
| 证据编号生成 | `backend/app/services/ingestion_service.py`; `backend/app/schemas/common.py` | `_save_evidence`; `new_id` | `_save_evidence` 中 `EvidenceRecord(evidence_id=new_id("evd"))` | 证明导入后生成 evidence_id 并保存原文 / 抽取结果 | 数据接入 |
| 结构化流水 / 通话解析 | `backend/app/services/information_extraction.py` | `parse_structured_content`, `map_rows_to_triples`, `_call_row_to_triples` | 行解析和 call/bank 分支 | 证明 CSV/XLSX 行级解析为资金、通话、时间关系 | 数据处理 |
| EvidenceMapBuilder | `backend/app/services/evidence_map_service.py` | `EvidenceMapBuilder.build`, `_build_documents`, `_build_passages_and_triples` | `build` 及两个 build 子函数 | 证明 documents/passages/triples/edges 的三层证据地图输出 | 案件证据地图 |
| 三层证据地图 API | `backend/app/api/v1/routes/graph.py` | `get_evidence_map` | `@router.get("/{case_id}/evidence-map")` | 证明图谱页有专用 EvidenceMap API | 案件证据地图 |
| G6 三层切换 | `frontend/src/views/Graph.vue`; `frontend/src/components/G6EvidenceGraph.vue` | `setMapLayer`, `setLayoutMode`, `renderEvidenceMapLayer`, `G6EvidenceGraph` | 图层按钮、layout 传参和 G6 初始化 | 证明前端存在文件层/片段层/原始层展示和 G6 渲染 | 图谱可视化 |
| 核心邻域 | `frontend/src/views/Graph.vue` | `buildCoreNeighborhoodSubgraph` | 函数主体 15-25 行 | 证明一跳邻域视图由前端从已加载图谱过滤生成 | 图谱支撑机制 |
| 邻域扩展 | `frontend/src/views/Graph.vue` | `buildExpandedNeighborhoodSubgraph` | 函数主体 15-25 行 | 证明二跳扩展由前端 BFS / 邻接计算生成 | 图谱支撑机制 |
| 多跳召回 | `frontend/src/views/Graph.vue` | `buildMultihopPathSubgraph`, `shortestPath` | 路径选择与最短路函数 | 证明当前多跳召回是前端路径演示实现 | 图谱支撑机制 |
| 子图交叉 | `frontend/src/views/Graph.vue` | `buildIntersectionSubgraph` | 函数主体 | 证明子图交叉是两个局部子图的交集 / 桥接展示 | 图谱支撑机制 |
| RetrievalPlanner 工具调用 | `backend/app/services/retrieval_planner_service.py` | `RetrievalPlannerService.run`, `_execute_tool`, `_tool_descriptions` | `run` 循环和 `_execute_tool` 分支 | 证明实现 planner、工具调用、评估和终止条件 | Agentic RAG |
| RetrievalSession 持久化 | `backend/app/storage/memory_store.py`; `retrieval_planner_service.py` | `_initialize_db`; `flush_case`; `RetrievalPlannerService.run` | retrieval_sessions / retrieval_steps 建表和写入 | 证明检索过程进入 SQLite | 可解释检索 |
| 智能分析会话 | `backend/app/services/analysis_service.py` | `create_thread`, `add_thread_message`, `delete_thread` | 会话创建和追问函数 | 证明支持新建对话、继续追问、删除和保存消息 | 智能分析 |
| @证据解析 | `backend/app/services/analysis_service.py` | `_resolve_evidence_mentions` | mention 正则和错误分支 | 证明当前 @ 机制是 @证据，不是 @技能包 | 边界说明 |
| GroundedSentence 溯源 | `backend/app/services/analysis_service.py`; `schemas/analysis.py` | `_ground_answer_sentences`; `GroundedSentence` | 句子打分和 passages/graph_paths 绑定 | 证明分析句可绑定证据片段和图谱路径 | 证据溯源 |
| portrait_facts 画像 | `backend/app/services/analysis_service.py` | `portrait_facts`, `_deepseek_grounded_portrait_facts`, `_facts_from_graph` | portrait_facts 主流程和 fallback | 证明画像事实由检索结果、图谱和证据片段生成 | 侦查画像 |
| PortraitFact 溯源 | `backend/app/schemas/analysis.py`; `frontend/src/views/Portrait.vue` | `PortraitFact`; `openTrace` | fact 字段与前端溯源抽屉 | 证明画像事实可点击查看来源 | 侦查画像 |
| 画像报告 claim | `backend/app/services/report_service.py`; `schemas/report.py` | `generate_portrait`, `_verify_claim`, `PortraitClaim` | claim 校验与 supporting_passages | 证明详细报告 claim 有证据绑定尝试 | 画像报告 |
| 图谱人工编辑 | `backend/app/services/graph_service.py`; `frontend/src/views/Graph.vue` | `apply_intervention`; `saveManualNode`, `saveManualEdge` | 后端干预和前端表单 | 证明支持增删改查/复核式图谱干预 | 人机协同 |
| 法律知识检索 | `backend/app/services/legal_knowledge_service.py` | `retrieve`, `_build_legal_docs` | retrieve 主体 | 证明法律知识可以按 query/offense_id 检索 | 法律知识库 |
| Rule Pack / Skills | `backend/app/services/analysis_skill_service.py`; `routes/analysis_skills.py` | `sync_registry`, `list_rule_packs`, `run_rule_pack` | registry 扫描和规则运行 | 证明后端规则包可注册、列出和运行 | 检察 Skills |

## 三、功能实现状态与边界说明

| 功能 | 当前状态 | 代码依据 | 是否可演示 | 风险 / 边界 | 支撑材料推荐表述 |
|---|---|---|---|---|---|
| 多源材料导入 | 已实现 | `ingestion_service.py`; `routes/ingestion.py` | 是 | `.doc` 暂不支持 | 支持文本、文件、多文件和 ZIP 导入，覆盖常用证据材料格式。 |
| 证据切片 | 已实现 | `information_extraction.extract_unstructured_triples`; `PassageRecord` | 是 | 切片规则较轻量 | 系统将材料拆成可检索、可溯源的证据片段。 |
| 结构化流水 / 通话解析 | 已实现 | `map_rows_to_triples`; `_call_row_to_triples` | 是 | 依赖列名和样本格式 | 结构化流水、通话、时间线可被行级解析并映射为关系。 |
| 实体识别 | 部分实现 | HippoRAG OpenIE; `ontology.py`; `algorithm.py` | 是 | LLM 输出和类型归一仍需优化 | 系统具备实体抽取和类型归一能力，但仍需人工复核。 |
| OpenIE / 事实关系抽取 | 部分实现 | `backend/app/hipporag`; `algorithm.py`; `information_extraction.py` | 是 | 非结构化抽取受模型质量影响 | 系统可从文本和结构化数据抽取事实关系，复杂关系需复核。 |
| 三层证据图谱 | 已实现 | `evidence_map_service.py`; `schemas/evidence_map.py`; `Graph.vue` | 是 | 文件/片段质量依赖抽取结果 | 形成文件层、片段层、事实关系层三种分辨率的证据地图。 |
| 图谱筛选 | 原型实现 | `Graph.vue` filter/tag 逻辑 | 是 | 部分 tag 来自图谱节点属性，质量受分类影响 | 支持按节点、关系和证据类型筛选局部图谱。 |
| 核心邻域 | 演示性实现 | `buildCoreNeighborhoodSubgraph` | 是 | 前端一跳过滤，不是后端图算法 | 可围绕目标节点展示直接关联网络。 |
| 邻域扩展 | 演示性实现 | `buildExpandedNeighborhoodSubgraph` | 是 | 前端二跳 BFS，非数据库查询 | 可围绕目标节点扩展外围关系。 |
| 多跳召回 | 演示性实现 | `buildMultihopPathSubgraph`; `shortestPath` | 是 | 图谱页按钮未直接调用 PPR | 可展示若干高价值关系路径，用于演示关系链发现。 |
| 子图交叉 | 演示性实现 | `buildIntersectionSubgraph` | 是 | 前端交集展示，非完整跨图算法 | 可比较两个局部子图的共同节点和桥接关系。 |
| PPR / PageRank-like 检索 | 部分实现 | HippoRAG / `analysis.trace` / `retrieval_planner_service.search_documents` | 是 | 主要用于 RAG/trace，不是图谱按钮实现 | 系统在检索和溯源链路中使用图增强召回能力。 |
| Agentic RAG | 部分实现 | `RetrievalPlannerService.run`; `_plan_next_round`; `_execute_tool` | 是 | fallback 占一定比例；不是官方 function calling | 系统实现了检索计划、工具调用、评估和过程保存的 Agentic RAG 原型。 |
| 检索过程持久化 | 已实现 | `MemoryStore` retrieval tables; `RetrievalSession/RetrievalStep` | 是 | JSON payload 存储 | 检索轮次、工具调用和召回摘要可持久化并回查。 |
| 检察分析 Skills | 部分实现 | `analysis_skill_service.py`; `analysis_skills.py`; packs 目录 | 后端可演示 | 前端页面未注册；导入/导出路由未见实现 | 已具备规则包注册、列表与运行能力，技能管理 UI 仍需补齐。 |
| @技能包 | 未见实现 | `_resolve_evidence_mentions` 只解析 @证据 | 否 | `@行为画像分析` 会被当成证据名 | 当前 @ 机制用于指定证据；@技能包属于规划增强。 |
| 法律知识库 | 部分实现 | `legal_knowledge_service.py`; `legal_knowledge.py`; `LegalKnowledge.vue` | 部分可演示 | 前端封装与后端 API 存在不一致 | 支持罪名模板、流程知识和法律知识检索，接口仍需统一。 |
| 智能分析 | 已实现 | `analysis_service.py`; `Intelligence.vue`; analysis threads API | 是 | 生成质量依赖模型和召回质量 | 支持假设验证、可疑资金流、多轮追问和证据溯源。 |
| 可疑资金流 | 部分实现 | `financial_flow` thread mode; `_financial_flow_candidates` | 是 | 暂无 GNN，主要是规则/LLM/RAG | 支持可疑资金流分析入口，当前以图谱与 RAG 分析为主。 |
| 画像生成 | 已实现 | `portrait_facts`; `Portrait.vue` | 是 | 生成依赖模型；可超时 | 可生成人物关系、行为事实和画像叙述。 |
| 画像报告 | 已实现 | `report_service.py`; `reports.py` | 是 | 句级支撑不是 100% 强绑定 | 可生成详细画像报告并尝试绑定证据支撑。 |
| 证据溯源 | 已实现 | `GroundedSentence`; `PortraitFact`; `PortraitClaim`; evidence detail API | 是 | 部分结论可能无可靠 passage | 支持分析句、画像事实和报告 claim 的证据回查。 |
| 人工编辑与复核 | 已实现 | `GraphService.apply_intervention`; `Graph.vue` | 是 | 主要面向当前图谱状态 | 支持节点、关系的人工增删改查和复核标记。 |
| 跨案碰撞 | 原型实现 | `/graph/merged`; `suspicion_analysis(cross_case)` | 部分可演示 | 尚非完整跨案索引系统 | 已有跨案合并图和 cross_case 疑点分析入口，属于原型能力。 |
| GNN / 异常资金识别 | 规划增强 | 未见 GNN 模型、训练或推理代码 | 否 | 不应宣称已实现 | 当前未见真实 GNN 异常识别代码，后续可作为增强方向。 |

## 四、运行验证报告

| 验证项 | 命令 / 路径 | 结果 | 失败原因 / 注意事项 | 是否影响演示 |
|---|---|---|---|---|
| 后端依赖安装是否可行 | `backend/requirements.txt` | 未在本轮重装；当前本机服务已运行 | 依赖安装需按 README 使用 Python 3.10 | 不影响当前已启动演示 |
| 后端启动命令是否正确 | `C:\Users\adm14\AppData\Local\Programs\Python\Python310\python.exe -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000` | `GET /api/v1/health` 返回 200 | 已有服务在 8000 运行 | 不影响 |
| 前端依赖安装是否可行 | `frontend/package-lock.json` | 未重装；当前依赖可 build | 如换机器需 `npm install` | 不影响当前机器 |
| 前端 build | `cd frontend; npm run build` | 通过 | Vite 提示 chunk 大于 500KB，为警告 | 不影响 |
| 前端 lint | `cd frontend; npm run lint` | 通过 | 使用 `tsc --noEmit` | 不影响 |
| 后端测试 | `cd backend; Python310 -m pytest -q` | 失败 | 当前 Python 环境未安装 pytest：`No module named pytest` | 不影响页面演示，但影响自动测试佐证 |
| SQLite 自动初始化 | `MemoryStore.__post_init__` / `_initialize_db` | 代码存在，当前服务可列案件 | SQLite 路径由 `STORAGE_DB_PATH` 控制 | 不影响 |
| 是否能导入 realcleantest | `data/realcleantest.zip`; 导入 API | 本轮未重新导入；当前已有 `case_632371a3a613`，42 份证据 | 重新导入可能触发模型/图谱构建耗时 | 不影响现有演示案 |
| `/` 页面 | `http://127.0.0.1:5173/` | 200 | - | 不影响 |
| `/graph` 页面 | `http://127.0.0.1:5173/graph` | 200 | - | 不影响 |
| `/intelligence` 页面 | `http://127.0.0.1:5173/intelligence` | 200 | - | 不影响 |
| `/portrait` 页面 | `http://127.0.0.1:5173/portrait` | 200 | - | 不影响 |
| `/legal-knowledge` 页面 | `http://127.0.0.1:5173/legal-knowledge` | 200 | 页面可访问，但部分前端 API 封装与后端不一致 | 基础展示不影响，上传/索引功能需校正 |
| `/api/v1/cases` | `GET http://127.0.0.1:8000/api/v1/cases` | 200，返回 4 个案件 | 包括 `case_632371a3a613` | 不影响 |
| `/api/v1/cases/{case_id}/evidence-map` | `GET .../case_632371a3a613/evidence-map` | 200 | 返回 documents/passages/triples | 不影响 |
| `/api/v1/cases/{case_id}/graph` | `GET .../case_632371a3a613/graph` | 200 | 返回 InvestigationGraph | 不影响 |
| `/api/v1/cases/{case_id}/analysis/threads` | `GET .../analysis/threads` | 200 | 存在已有会话 | 不影响 |
| `/api/v1/cases/{case_id}/analysis/portrait-facts` | `POST .../analysis/portrait-facts` | 20 秒内超时 | 该接口会触发检索 / 模型生成，依赖模型与耗时 | 若无缓存会影响现场即时生成，建议提前生成缓存 |
| `/api/v1/cases/{case_id}/analysis/retrieval-sessions/{session_id}` | `GET .../retrieval-sessions/rsess_41b9b55b1d6c` | 200 | 需从已有 thread message 中取得 session_id | 不影响 |

离线可验证部分：前端页面、图谱 API、EvidenceMap、Graph、案件列表、已存在 retrieval session。依赖模型 Key 的部分：新建智能分析、重新生成 portrait_facts、详细报告生成、HippoRAG/OpenIE 新跑图谱。比赛演示建议提前准备已分析案件和缓存结果。

## 五、高风险点真实结论

| 风险点 | 真实情况 | 代码依据 | 支撑材料推荐说法 | 不建议说法 |
|---|---|---|---|---|
| @技能包是否真的实现 | 未见实现；当前 @ 解析为证据 mention | `analysis_service.py:_resolve_evidence_mentions` | “当前 @ 用于指定证据；技能包通过后端 rule_packs API 与规划工具接入，@技能包是后续增强方向。” | “已支持 @行为画像分析 选择技能包。” |
| `@行为画像分析` 会怎样 | 会被解析为 `@证据`，若不存在同名证据会报“未找到 @ 指定的证据” | `_resolve_evidence_mentions` loose mention 正则 | “演示任务如需写技能名，应避免使用 @，或仅作为文字说明。” | “@技能包可直接驱动分析。” |
| 子图交叉、多跳召回是否后端图算法 | 当前图谱页主要是前端 Graph.vue 对已加载图谱做过滤、BFS、路径和交集展示 | `Graph.vue:buildMultihopPathSubgraph/buildIntersectionSubgraph` | “图谱页提供局部子图、路径和交叉视角，用于演示关系发现过程。” | “已实现后端图数据库级碰撞分析。” |
| PPR 是否用于图谱页 | 未见图谱按钮直接调用 PPR；PPR/trace 主要在 RAG 检索和溯源链路 | `retrieval_planner_service.search_documents`; `analysis.trace`; HippoRAG | “检索/溯源链路使用图增强召回，图谱页提供可视化局部操作。” | “图谱页核心邻域按钮直接使用 PPR。” |
| 法律知识库 API 是否一致 | 不完全一致。后端有 `/offense-templates`, `/procedure-flows`, `/procedure-flows/upload`, `/retrieve`；前端封装还包含未见后端路由的 `/index`, `/crimes`, `/processes`, `/upload` | `routes/legal_knowledge.py`; `frontend/src/api/backend.ts` | “法律知识后端能力已具备，前端部分接口需统一。” | “法律知识库前后端已完全闭环。” |
| SkillLibrary.vue 是否有路由 | 未在 `frontend/src/router.ts` 注册 | `router.ts` 只注册 Dashboard、LegalKnowledge、Graph、Portrait、Intelligence、Chat | “技能库页面文件已存在，路由接入与导入导出接口仍需补齐。” | “技能库页面可完整使用。” |
| analysis contexts API 是否存在 | 前端 API 封装存在，后端 routes 未见 `/analysis/contexts` | `frontend/src/api/backend.ts`; `backend/app/api/v1/routes` | “分析上下文调试页/API 属于规划或待接入能力。” | “分析上下文调试页已完整实现。” |
| GNN / 异常资金识别 | 未见 GNN 模型、训练、推理代码 | 全仓库未见 GNN 相关实现 | “可疑资金流当前基于图谱、规则和 RAG 分析；GNN 是后续增强。” | “已使用 GNN 识别异常资金。” |
| 跨案碰撞是否完整实现 | 原型实现：有 merged graph 和 suspicion cross_case 入口 | `GraphService.get_merged_graph`; `analysis_service.suspicion_analysis(cross_case)` | “具备跨案合并图和疑点分析入口，属于原型能力。” | “已形成完整跨案碰撞平台。” |

## 六、建议替换进正式材料的稳妥表述

1. **关于图谱支撑机制**：  
   系统提供基于案件证据图谱的局部关系观察能力，包括核心邻域、邻域扩展、多跳路径和子图交叉视角。当前图谱页实现主要用于演示和交互分析，后续可进一步下沉为后端图算法和图数据库查询。

2. **关于 Agentic RAG**：  
   系统实现了检索计划器、工具调用、文档 / 图谱 / 法律知识检索和过程持久化。模型规划不可用时，系统会回退到固定工具计划，以保证演示链路稳定。

3. **关于 Skills**：  
   系统后端已具备规则包注册、列表和运行能力，当前主要服务于规则 finding 和分析约束；完整的前端技能库管理、@技能包选择和技能回放仍为后续增强。

4. **关于证据溯源**：  
   系统为智能分析句、画像事实和报告 claim 提供 passage、graph path 或 evidence_id 级溯源。对于无法可靠匹配证据的生成句，系统不应宣称强支撑，而应作为待复核内容。

5. **关于可疑资金流与 GNN**：  
   当前可疑资金流分析以结构化流水、图谱关系、规则和 RAG 解释为主；GNN 异常识别尚未落地，可作为后续在更多标注样本基础上的增强方向。


