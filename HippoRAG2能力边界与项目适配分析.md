# HippoRAG 2 能力边界与项目适配分析

## 0. 本地仓库情况

已将官方仓库克隆到本项目目录：

```text
C:\Users\adm14\Desktop\law_project\HippoRAG
```

官方仓库：

```text
https://github.com/OSU-NLP-Group/HippoRAG
```

仓库 README 当前标题为：

```text
HippoRAG 2: From RAG to Memory
```

从 README、`src/hipporag/HippoRAG.py`、`src/hipporag/information_extraction/openie_openai.py`、`src/hipporag/embedding_store.py` 和 `src/hipporag/utils/config_utils.py` 看，HippoRAG 2 的定位不是普通知识图谱系统，也不是前端图谱可视化工具，而是一个面向 LLM 的 **记忆增强型 RAG 框架**。

## 1. 一句话结论

HippoRAG 2 可以用于本项目，但不应该作为整个系统的核心架构。

更准确地说，它适合放在我们系统中的：

```text
证据文本记忆层 / 语义检索层 / 报告生成前置检索层
```

不适合替代：

```text
证据解析层
侦查图谱数据库
结构化资金分析
规则化碰撞引擎
前端可视化系统
法律判断与侦查结论
```

它对我们最有价值的地方，是帮助系统在大量笔录、文书、说明材料、聊天记录、OCR 文本中，做跨文档、多跳、有关联感的语义检索，为后续画像报告和参考建议提供更可靠的证据上下文。

## 2. HippoRAG 2 到底是什么

HippoRAG 2 官方将自己描述为：

```text
a powerful memory framework for LLMs
```

它的核心目标是增强 LLM 对新知识之间连接关系的识别和利用能力，尤其关注：

- factual memory：事实记忆
- sense-making：复杂上下文整合
- associativity：多跳关联检索

这和普通 RAG 的区别在于：

- 普通 RAG 更多是“向量相似度召回文本片段”。
- HippoRAG 2 会先从文本中抽实体和关系三元组，构建内部图结构，再通过事实检索、实体链接和图搜索找到更相关的文档片段。

所以它可以理解为：

```text
向量检索 + OpenIE 信息抽取 + 内部事实图 + 个性化 PageRank 检索 + LLM 问答
```

它不是一个面向用户展示的业务系统，而是一个底层检索与记忆框架。

## 3. 核心工作流程

根据 `src/hipporag/HippoRAG.py`，它的主流程大致是：

```text
输入 docs
-> chunk embedding
-> OpenIE 抽取实体与三元组
-> entity embedding
-> fact embedding
-> 构建 igraph 图
-> 查询时先检索相关 fact
-> rerank fact
-> 将 fact 中实体映射到图节点
-> personalized PageRank
-> 返回相关文档片段
-> LLM 基于片段生成答案
```

### 3.1 离线索引阶段

调用：

```python
hipporag.index(docs=docs)
```

它会做几件事：

- 将原始文档保存为 chunk embedding。
- 使用 OpenIE 抽取命名实体。
- 使用 OpenIE 抽取三元组。
- 为实体、事实分别建立 embedding store。
- 将实体节点、文档 chunk 节点和事实关系写入 igraph。

对应源码位置：

```text
src/hipporag/HippoRAG.py
index()
add_fact_edges()
add_passage_edges()
add_synonymy_edges()
augment_graph()
```

### 3.2 在线检索阶段

调用：

```python
hipporag.retrieve(queries=queries)
```

检索流程包括：

- 对 query 编码。
- 计算 query 与 fact embedding 的相似度。
- 选出候选 facts。
- 使用 rerank 过滤事实。
- 将相关 fact 中的实体作为图搜索起点。
- 对内部图执行 personalized PageRank。
- 返回相关文档片段。

对应源码位置：

```text
src/hipporag/HippoRAG.py
retrieve()
get_fact_scores()
rerank_facts()
graph_search_with_fact_entities()
run_ppr()
```

### 3.3 问答生成阶段

调用：

```python
hipporag.rag_qa(queries=queries)
```

它会先检索，再把检索到的文档片段交给 LLM 生成回答。

对应源码位置：

```text
src/hipporag/HippoRAG.py
rag_qa()
qa()
```

## 4. 它的核心能力

## 4.1 多跳语义检索能力

HippoRAG 2 最强的地方是多跳检索。

例如普通向量检索可能只能找到和问题表面相似的文本，但 HippoRAG 2 会尝试利用实体和三元组之间的连接关系，找到间接相关的证据片段。

这对我们项目很有价值。

在检察侦查场景里，很多线索不是一跳关系，而是：

```text
嫌疑人 -> 密切关系人 -> 账户 -> 企业 -> 资金流出
```

或者：

```text
笔录中的人名 -> 通联记录中的号码 -> 资金流水中的账户 -> 文书中的案件节点
```

HippoRAG 2 的多跳检索思想，适合用来增强这类跨材料查找。

## 4.2 跨文档上下文整合能力

它可以把多个文档 chunk 放入同一记忆索引中。

对我们来说，可以把以下材料转成文本后作为 docs：

- 讯问笔录
- 谈话记录
- 判决书
- 裁定书
- 搜查笔录
- 扣押清单
- 涉案财物说明
- OCR 文本
- 聊天记录文本
- 银行流水的文本化摘要
- 通联详单的文本化摘要

当用户问：

```text
张某与李某之间是否存在资金和行为上的异常关联？
```

HippoRAG 2 可以帮助召回多个材料中的相关片段，再交给 LLM 归纳。

## 4.3 实体和事实抽取能力

HippoRAG 2 内部会调用 OpenIE，先做 NER，再做三元组抽取。

源码中 `openie_openai.py` 的流程是：

```text
ner()
triple_extraction()
batch_openie()
```

它默认提示词比较通用，偏英文开放域。

对我们来说，这个能力可以借，但必须改造 prompt。

默认抽取：

```text
named_entities
triples
```

我们需要改成更适合侦查场景的实体和关系类型，例如：

实体：

- 犯罪嫌疑人
- 密切关系人
- 司法工作人员
- 案件
- 账户
- 交易
- 企业
- 房产
- 车辆
- 时间节点
- 行为事件
- 证据材料

关系：

- 转账给
- 控制账户
- 共同出现
- 频繁联系
- 上下级关系
- 参与办理
- 资金中转
- 关联企业
- 证据指向
- 发生于

## 4.4 增量更新与删除能力

仓库测试文件 `tests_openai.py` 中包含：

- 首次索引
- 再次加载
- 新增文档
- 删除文档

源码中也有：

```text
index()
delete()
```

这说明它具备一定的增量记忆能力。

对我们项目来说，这意味着：

- 新增一份笔录后，可以追加索引。
- 新增一份流水摘要后，可以追加索引。
- 发现材料误导入后，可以删除对应文档。

但这不等于完整的数据治理系统，仍然需要我们自己的证据元数据管理和来源追溯。

## 4.5 OpenAI-compatible 与本地部署可能性

README 显示它支持：

- OpenAI 模型
- OpenAI-compatible LLM endpoint
- OpenAI-compatible embedding endpoint
- vLLM 本地服务
- Azure
- Bedrock

这对我们很重要。

因为检察侦查场景涉及敏感数据，方案表达上应强调：

```text
可接入本地化或私有化部署的 OpenAI-compatible 模型服务
```

不过实际落地时，HippoRAG 2 的依赖比较重，尤其是：

```text
torch
transformers
vllm
gritlm
python_igraph
```

如果没有 GPU 或可用的模型服务，完整跑起来会比较困难。

## 5. 能力边界

## 5.1 它不是证据解析器

HippoRAG 2 接收的是已经整理好的文本 docs。

它不会替我们处理：

- PDF 解析
- Word 解析
- Excel 表格读取
- 银行流水字段映射
- OCR 图片识别
- 通联详单结构化
- 文件来源标注

所以在我们的系统里，HippoRAG 2 必须放在证据接入和清洗之后。

正确位置是：

```text
证据解析完成后 -> 将文本化材料送入 HippoRAG 2
```

而不是：

```text
原始文件 -> HippoRAG 2
```

## 5.2 它不是业务图谱数据库

这是最重要的边界。

HippoRAG 2 内部确实有 graph，但这个 graph 是为了检索服务的。

它的图节点主要是：

- passage node
- entity node

边主要来自：

- OpenIE 三元组
- passage-entity 连接
- synonymy edge

这和我们项目中的侦查图谱不是一回事。

我们的侦查图谱需要表达：

- 人物
- 账户
- 交易
- 案件节点
- 行为事件
- 时间线
- 证据来源
- 风险标签
- 关系类型
- 关系强度
- 可视化坐标
- 查询过滤条件

所以不能说：

```text
接入 HippoRAG 2 后，我们就有侦查图谱了。
```

更稳的说法是：

```text
HippoRAG 2 可作为证据语义记忆和多跳检索层；侦查图谱仍由我们自己的图谱数据模型和业务数据库承载。
```

## 5.3 它不是规则碰撞引擎

HippoRAG 2 能增强“语义关联检索”，但它不会自动完成我们题目里的业务碰撞。

例如这些仍然要我们自己做：

- 人物 x 资金
- 人物 x 行为
- 资金 x 时间
- 行为 x 时间
- 关系网络 x 资金网络

HippoRAG 2 可以帮助找到相关证据上下文，但不能替代规则判断。

例如：

```text
04-06 至 04-10 期间是否出现资金异动与通联频次同步上升？
```

这类问题需要结构化数据表和时间序列逻辑来算。

HippoRAG 2 可以补充：

```text
召回与该时间窗口、人员、账户相关的笔录、聊天记录和文书片段。
```

## 5.4 它不是法律判断模型

HippoRAG 2 只能帮助检索和生成回答。

它不能：

- 自动认定犯罪
- 自动判断罪名成立
- 自动替代侦查人员决策
- 自动生成具有法律效力的结论

在我们项目中，它只能作为：

```text
辅助检索、辅助归纳、辅助生成参考信息
```

## 5.5 中文司法材料适配不确定

从默认 prompt 看，HippoRAG 2 的 NER 和 triple extraction 是开放域英文示例。

对于中文司法材料，它可能出现：

- 实体抽取不稳定
- 三元组关系过于泛化
- 法律术语理解不准
- 姓名、职务、单位、案号混淆
- 同名不同人难以区分
- 表格型数据转文本后丢失字段关系

如果要用于本项目，必须做中文侦查场景 prompt 改造。

## 6. 与我们项目的对应关系

## 6.1 可以接入的位置

我们当前系统设计主链路是：

```text
证据接入 -> 处理层 -> 分析与碰撞层 -> 输出生成层 -> 展示与交互层
```

HippoRAG 2 最适合放在：

```text
处理层之后，输出生成层之前
```

更具体地说，是：

```text
证据解析 / 清洗 / 文本化
-> HippoRAG 2 语义记忆索引
-> 根据问题召回相关证据片段
-> LLM 生成报告、画像和建议
```

## 6.2 对证据接入层的帮助

帮助有限。

证据接入层仍然需要我们自己完成：

- 文件上传
- 格式识别
- 文本提取
- 表格解析
- 字段标准化
- 来源标注

HippoRAG 2 只接收处理后的文本。

它不能直接解决多格式文件导入问题。

## 6.3 对处理层的帮助

有一定帮助，但不是全量替代。

可以帮助：

- 从文本中抽实体
- 从文本中抽关系三元组
- 建立语义记忆图
- 建立 chunk/entity/fact 三类 embedding

不能替代：

- 我们自己的侦查领域本体
- 结构化字段模型
- 账户、交易、行为、时间线的数据模型
- 面向前端展示的业务图谱

## 6.4 对分析与碰撞层的帮助

适合做辅助证据召回。

例如规则碰撞已经发现：

```text
张某与李某在关键时间窗口存在高频通联，同时账户A向李某分拆转账。
```

这时可以向 HippoRAG 2 查询：

```text
查找与张某、李某、账户A、04-06至04-10相关的笔录、聊天记录和文书片段。
```

然后把召回结果提供给报告生成模块。

它不能直接完成碰撞规则本身。

## 6.5 对输出生成层的帮助

这是最适合使用 HippoRAG 2 的地方。

我们的输出生成层包括：

- 专题报告
- 信息画像
- 参考建议

这些输出最怕 LLM 空泛编写。

HippoRAG 2 可以作为报告生成前的证据检索器，让 LLM 基于召回证据写报告。

推荐链路：

```text
用户选择报告类型
-> 系统生成检索问题
-> HippoRAG 2 召回相关证据片段
-> 结构化整理片段
-> LLM 生成报告
-> 附带证据来源
```

例如：

```text
资金流动报告
-> 检索“张某、李某、账户A、分拆转账、企业X、离岸账户”
-> 召回银行流水摘要、聊天记录、笔录、企业登记说明
-> 生成资金链说明
```

## 7. 建议的系统架构接入方式

## 7.1 我们自己的系统仍然是主系统

建议主系统保持：

```text
前端 React/Vite
后端 API
结构化数据库
文本检索索引
侦查图谱数据库
时间序列数据
规则碰撞模块
LLM 输出模块
```

HippoRAG 2 作为独立服务接入：

```text
HippoRAG Memory Service
```

## 7.2 推荐架构

```text
证据上传
-> 文件解析
-> 文本化与结构化
-> 结构化数据入库
-> 侦查图谱入库
-> 文本片段送入 HippoRAG 2
-> 用户发起报告/画像/建议生成
-> 后端调用 HippoRAG 2 retrieve
-> 获得相关证据片段
-> LLM 生成结构化输出
-> 前端展示
```

## 7.3 HippoRAG 2 可以提供的接口

建议我们封装以下后端接口：

```text
POST /memory/index
POST /memory/retrieve
POST /memory/qa
POST /memory/delete
GET  /memory/status
```

其中：

`/memory/index`

- 输入解析后的文本片段
- 写入 HippoRAG 2

`/memory/retrieve`

- 输入查询问题
- 返回相关证据片段

`/memory/qa`

- 输入问题
- 返回 RAG 生成回答

`/memory/delete`

- 删除误导入的证据片段

`/memory/status`

- 返回已索引文档数、实体数、事实数、图节点数等状态

## 8. 本项目中最适合的应用场景

## 8.1 场景一：报告生成前的证据召回

这是最推荐的用途。

例如生成“人物关系报告”时：

```text
查询：张某与李某、王某之间有哪些关系、资金往来和关键事件？
```

HippoRAG 2 召回相关笔录、文书、聊天记录、流水摘要。

再由 LLM 生成报告。

## 8.2 场景二：画像生成的证据补充

信息画像不能只依靠结构化字段。

例如密切关系人画像需要：

- 与嫌疑人的关系
- 是否参与资金链
- 是否在关键行为中出现
- 是否有笔录或聊天内容支持

HippoRAG 2 可以辅助查找这些证据上下文。

## 8.3 场景三：侦查人员自然语言查询

可以在 demo 后续加一个“自然语言问证据”入口：

```text
查询张某和李某之间是否存在资金往来与高频通联的重合证据。
```

系统返回：

- 相关证据片段
- 来源材料
- 相关人物
- 相关时间
- 摘要说明

这会让 demo 更像智能分析平台。

## 8.4 场景四：跨材料多跳线索提示

例如：

```text
某笔交易对应的收款人是否在笔录或通联记录中出现？
```

HippoRAG 2 的多跳检索逻辑适合做这种跨文本材料的关联召回。

## 9. 不建议使用的场景

## 9.1 不建议用于资金流水精确分析

资金流水要靠结构化表和规则。

例如：

- 金额统计
- 时间窗口聚合
- 拆分转账检测
- 循环转账检测
- 账户穿透

这些不适合交给 HippoRAG 2。

## 9.2 不建议用于前端图谱展示

HippoRAG 2 的图是内部检索图，不适合直接展示给评委或侦查人员。

前端图谱仍应使用我们自己的业务图谱数据。

## 9.3 不建议用于法条适用判断

它不是法律推理框架，不应承担罪名判断、构成要件判断或法律意见生成。

## 9.4 不建议作为短期 demo 的第一优先级

如果时间很紧，优先级应该是：

1. React 前端 demo
2. mock 数据完整闭环
3. 证据接入页面
4. 图谱展示页面
5. 碰撞分析页面
6. 画像与报告页面
7. 再接 HippoRAG 2 作为增强检索模块

原因是 HippoRAG 2 完整部署依赖较重，短期内不一定稳定。

## 10. 技术落地难点

## 10.1 依赖重

`requirements.txt` 中包含：

```text
torch
transformers
vllm
gritlm
python_igraph
openai
litellm
```

这意味着：

- 本地环境可能较大。
- 无 GPU 时本地模型方案不现实。
- 使用 OpenAI-compatible API 会更快。
- 如果要私有化部署，需要额外模型服务和硬件支持。

## 10.2 中文法律材料 prompt 需要重写

默认 prompt 偏开放域英文材料。

我们至少需要改：

```text
src/hipporag/prompts/templates/ner.py
src/hipporag/prompts/templates/triple_extraction.py
```

改造方向：

- 中文输出
- 侦查实体类型
- 关系类型约束
- 证据来源保留
- 同名人员消歧
- 时间、金额、账户字段保留

## 10.3 结构化数据不应直接文本化后丢失字段

银行流水、通联详单、出入记录等数据，如果直接转成自然语言文本，可能丢失精确字段分析能力。

更好的方式是双轨：

```text
结构化表用于精确计算
文本化摘要用于 HippoRAG 2 语义检索
```

例如一条流水同时进入：

```text
transactions table
```

和：

```text
“2026-04-06 21:13，账户A向李某转账48000元，系统标记为分拆转账。”
```

## 10.4 证据来源追溯需要我们自己加强

HippoRAG 2 返回的是 docs 片段。

我们要保证每个 doc 片段带上：

- 文件名
- 文件类型
- 来源单位
- 页码或行号
- 解析时间
- 证据编号

否则生成报告时很难做到“有据可查”。

## 11. 推荐的最小 PoC

如果要验证 HippoRAG 2 是否适合本项目，不建议一开始就接全量系统。

建议做一个最小 PoC。

## 11.1 PoC 输入

准备 20-50 条文本化证据片段：

- 5 条笔录摘要
- 10 条银行流水摘要
- 10 条通联记录摘要
- 5 条行为轨迹摘要
- 5 条文书摘要

每条证据片段都加来源标记，例如：

```text
[来源: 银行流水_张某_2026Q1.xlsx, 行号: 12]
2026-04-06 21:13，账户A向李某转账48000元，备注为空，系统标记为分拆转账。
```

## 11.2 PoC 查询

准备 5 个查询：

```text
张某和李某之间有哪些资金往来？
企业X与离岸账户之间有什么关系？
04-06至04-10期间有哪些异常行为？
李某是否同时出现在资金链和通联链中？
请找出支持“张某与李某关系密切”的证据片段。
```

## 11.3 PoC 输出

看三个指标：

- 能不能召回正确证据片段
- 能不能跨材料找到间接关系
- 召回结果是否适合报告生成

## 11.4 PoC 成功标准

如果它能稳定做到：

```text
查询一个侦查问题 -> 召回 3-5 条相关证据片段 -> 片段来自不同材料 -> 能支撑报告生成
```

那么就值得接入项目。

如果它只能召回表面相似文本，或者经常漏掉关键字段，那就只保留为“后续扩展方向”。

## 12. 对当前 demo 的建议

我们现在已经开始做 React demo。

短期内建议这样规划：

## 12.1 第一阶段：先不接 HippoRAG 2

先完成：

- 证据接入页面
- 图谱穿透页面
- 分析碰撞页面
- 信息画像页面
- 报告建议页面

用 mock 数据跑通演示链路。

## 12.2 第二阶段：加一个“证据语义检索”页面

在 demo 中新增一个页面：

```text
证据问答 / 语义检索
```

页面包含：

- 自然语言输入框
- 返回证据片段列表
- 证据来源
- 相关实体
- 相关事实
- 可加入报告按钮

这个页面最能体现 HippoRAG 2 的价值。

## 12.3 第三阶段：将报告生成接入 HippoRAG 2

让报告生成流程从：

```text
mock 文案
```

升级为：

```text
专题问题 -> HippoRAG 2 检索 -> LLM 归纳 -> 报告生成
```

## 13. 能否写进 PPT 或申报表

可以写，但不要写得过满。

建议写法：

```text
在证据语义检索层，项目可引入 HippoRAG 2 等记忆增强型 RAG 框架，将解析后的文本证据构建为可检索的实体-事实记忆网络，提升跨材料、多跳线索召回能力，为画像报告和参考建议生成提供证据支撑。
```

不要写：

```text
我们使用 HippoRAG 2 完成全部图谱构建和侦查推理。
```

原因是这不准确，也容易被追问。

## 14. 最终建议

我的结论是：

```text
HippoRAG 2 值得研究，也适合接入，但应作为“证据语义记忆与多跳检索层”，而不是替代系统主架构。
```

在本项目中的定位应为：

- 增强报告生成的证据支撑
- 增强画像生成的跨材料依据
- 增强自然语言证据查询
- 辅助发现间接关联

但核心系统仍应由我们自己的：

- 证据接入
- 结构化数据表
- 侦查业务图谱
- 时间序列组织
- 规则碰撞
- 前端交互

共同支撑。

## 15. 建议优先级

当前阶段建议优先级：

1. 继续完成 React demo 主流程。
2. 设计 mock 证据数据和结构化数据。
3. 做一个“证据语义检索”页面。
4. 用小样本证据片段验证 HippoRAG 2。
5. 如果效果稳定，再将其接入报告生成链路。

这样最稳，也最符合比赛展示节奏。

