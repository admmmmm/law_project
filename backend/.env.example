APP_NAME=Procuratorial Investigation Portrait Backend
APP_ENV=dev
API_PREFIX=/api/v1
# stub keeps the API runnable; hipporag lazily loads need/HippoRAG/src when deps are ready.
ALGORITHM_PROVIDER=stub
MEMORY_AUTO_WRITEBACK=false
